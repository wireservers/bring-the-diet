# Next.js standalone output relies on the file tracer to resolve Next's own
# runtime deps (styled-jsx, @swc/helpers, etc.). pnpm's default symlinked
# layout hides these from the tracer, so the deployed bundle crashes on cold
# start. Flatten node_modules like npm so the tracer + Node resolver agree.
node-linker=hoisted
