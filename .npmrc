# Next.js standalone output's file tracer can't follow pnpm's deep symlink tree
# for some of Next's own runtime deps (styled-jsx in particular). Hoist them
# to the workspace root node_modules so the trace + require resolution both
# succeed in the deployed bundle.
public-hoist-pattern[]=*styled-jsx*
public-hoist-pattern[]=*@next/swc*
