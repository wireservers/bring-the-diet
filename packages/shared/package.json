{
  "name": "@bringthediet/shared",
  "private": true,
  "version": "0.0.1",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'no lint configured'",
    "build": "echo 'no build configured'"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
